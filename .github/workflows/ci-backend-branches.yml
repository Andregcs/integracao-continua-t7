<<<<<<< HEAD
name: CI Back-end

on:
    push:
        branches:
            - 'main'
        paths:
            - 'sgcmapi/**'
    workflow_dispatch:
    pull_request:
      paths:
        - 'sgcmapi/**'

jobs:
  build:
      runs-on: ubuntu-latest
      steps:
          - uses: actions/checkout@v5
          - name: Set up JDK 17
            uses: actions/setUp-java@v5
            with:
              java-version: '17'
              distribution: 'temurin'
          - name: Maven Compile/Test
            working-directory: sgcmapi/
            run: mvn compile test
  
=======
name: CI Back-end (Branches)

on:
  push:
    branches:
      - '**'
      - '!main'
    paths:
      - 'sgcmapi/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'
      - name: Maven Compile/Test
        working-directory: sgcmapi/
        run: mvn compile test
  merge:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Merge Branch
        uses: devmasx/merge-branch@1.4.0
        with:
          type: now
          target_branch: main
          github_token: ${{ github.token }}
>>>>>>> 9e6dcef49c13ff6d78f074d6ff049addb191b887
